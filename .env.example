# ── LLM Backend ──────────────────────────────────────────────────────────────
# "cloud"  → use a hosted API (recommended — no GPU needed)
# "local"  → use local HuggingFace models (requires GPU or patience)
LLM_BACKEND=cloud

# ── Cloud LLM Provider ────────────────────────────────────────────────────────
# Options: groq | openrouter | google | ollama
CLOUD_LLM_PROVIDER=groq

# ── Groq (recommended — free, very fast) ─────────────────────────────────────
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile
# Faster / lighter alternative:
# GROQ_MODEL=llama-3.1-8b-instant

# ── OpenRouter (alternative free tier) ───────────────────────────────────────
# CLOUD_LLM_PROVIDER=openrouter
# OPENROUTER_API_KEY=your_key_here
# OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# ── Google Gemini (alternative) ──────────────────────────────────────────────
# CLOUD_LLM_PROVIDER=google
# GOOGLE_API_KEY=your_key_here
# GOOGLE_MODEL=gemini-2.0-flash

# ── Ollama (local, no API key needed) ────────────────────────────────────────
# CLOUD_LLM_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=qwen2.5:3b
# Other available: qwen2.5:1.5b  gemma4:e4b

# ── Paths ─────────────────────────────────────────────────────────────────────
DATA_ROOT=data
ARTIFACTS_ROOT=artifacts
