# ── Data ───────────────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# ── ML / NLP ───────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
scipy>=1.11.0
nltk>=3.8.1
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
transformers>=4.41.0,<5
# CPU-only torch — sentence-transformers needs it for all-MiniLM-L6-v2 embeddings.
# CPU version saves ~1.5GB vs the default CUDA build (no GPU in Docker).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0

# ── API ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20
pydantic>=2.0.0
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0

# ── Utilities ──────────────────────────────────────────────────────────────
joblib>=1.4.0
requests>=2.32.0
pypdf>=5.4.0
python-dotenv>=1.0.0
polars>=1.8.0

# NOT included (HuggingFace microservice only, not the main app):
# gradio, gradio_client, huggingface_hub, spaces
